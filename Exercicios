Crie um aplicativo de formulário que leia os seguintes dados de usuário: nome completo, nome de usuário e senha (2 vezes) e após o clique em um botão simule o cadastro do mesmo, mostre uma mensagem curta com um ID (um inteiro) e altere a tela para que um novo campo com ID seja mostrado para o usuário. É necessário que o botão seja desabilitado também e que obrigatoriamente um dos métodos vistos acima seja utilizado. Após isso, faça o upload do seu projeto no ambiente virtual da disciplina.
